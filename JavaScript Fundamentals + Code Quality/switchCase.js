let browser = "Edge";

if (browser === "Edge") {
    console.log("Youve got an Edge!");
} else if (browser === "Chrome" || browser === "Firefox" || browser === "Safari" || browser === "Opera") {
    console.log("Okay we support these browsers too")
} else {
    console.log("We hope this page looks ok!")
}